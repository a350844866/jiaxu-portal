import {
  HeartPulse, Activity, ListVideo, Play, Download,
  Clapperboard, Film, Captions, Search, House,
  BarChart3, Images, CloudUpload, FolderOpen, Package,
  HeartHandshake, Container, Server, Globe, Archive,
  PanelLeft, Rocket, Network, Database, MessageSquare,
  Code2, Home, Cloud, LayoutDashboard, Building2,
  CandlestickChart,
} from "lucide-react"
import type { LucideIcon } from "lucide-react"

/** Only the icons actually referenced in services-data / categories-data */
export const iconMap: Record<string, LucideIcon> = {
  HeartPulse, Activity, ListVideo, Play, Download,
  Clapperboard, Film, Captions, Search, House,
  BarChart3, Images, CloudUpload, FolderOpen, Package,
  HeartHandshake, Container, Server, Globe, Archive,
  PanelLeft, Rocket, Network, Database, MessageSquare,
  Code2, Home, Cloud, LayoutDashboard, Building2,
  CandlestickChart,
}
